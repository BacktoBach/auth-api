{
  "name": "auth-api",
  "version": "1.0.0",
  "description": "REST API xác thực người dùng bằng JWT, bcrypt và RBAC",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check src/app.js && node --check src/controllers/authController.js && node --check src/controllers/userController.js && node --check src/services/authService.js && node --check src/services/userService.js && node --check src/middlewares/authMiddleware.js && node --check src/utils/token.js",
    "test": "node --test"
  },
  "keywords": [
    "jwt",
    "authentication",
    "rbac",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
